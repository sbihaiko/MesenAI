# F12.16 — emptiness probes as a last anchor pass (2026-09-22)

Scope and stop rule live in the PRD (Part A, row F12.16); the decision is
ADR-0223, accepted 2026-09-22, option A. Prerequisite: F12.15's split
overdraw tool (shipped, main), so the measure below is `erased background`
alone, on ADR-0221's Punch-Out!! route.

## What shipped

- `Core/NES/HdPacks/ScreenStitcher.h`/`.cpp` — `FlatRunColumns` (host-free:
  the grid columns a flat run spans, tile-boundary aligned to `fineX`);
  `AnchorChoice::UsedEmptinessProbe` (new counter); `SelectScreenAnchors`
  now explicitly excludes `Usage == UINT32_MAX` candidates from the
  stable/wide passes' pool (`order`), and, only when the stable/wide result
  still leaves rivals standing, runs one more greedy pass over the stable
  pool plus the flat cells no variant changes (`stableSubset`, factored out
  of the existing ADR-0159 §1 filter and reused for both pools) — kept only
  if it separates strictly more rivals than the previous best.
- `Core/NES/HdPacks/HdPackBuilder.h` — `AppendFlatAnchorCells` (new inline
  template method; host-bound, so it stays out of ScreenStitcher): for every
  flat run `CaptureScreen` would otherwise drop, expands it to every grid
  cell it covers and pushes an `AnchorCandidate` with `Usage = UINT32_MAX`
  plus a synthetic `ScreenRun` (same tile, `X` moved to the cell) into the
  same `pending.Cells`/`pending.Candidates` vectors the ranked, non-flat
  candidates use — so `choice.Picked` indices still resolve correctly when
  `FinalizeScreenAnchors` builds the `<background>`'s conditions.
- `Core/NES/HdPacks/HdPackBuilder.cpp` — `CaptureScreen` calls
  `AppendFlatAnchorCells` after building `ranked`/`pending.Candidates` as
  before (isFlat exclusion untouched); `FinalizeScreenAnchors` adds
  `emptinessProbeScreens` and the log clause "N screen(s) gated on an
  emptiness probe". Net **0 lines** (2426 -> 2428 against the ADR-0137
  ceiling of 2428, not raised — the loop itself lives in the header).
- `scripts/core_unit_tests.cpp` — new tests, cited as ADR-0223 (BlocoP5):
  `TestAnchorEmptinessProbeSeparatesWhenOnlyFlatCellDoes`,
  `TestAnchorNonFlatCellSeparatesWithoutAProbe`,
  `TestAnchorFlatCellsNeverAppearInStableOrWideOutput`,
  `TestAnchorProbeOnACellAVariantChangesIsNeverChosen` — a synthetic
  `GridFrame` pair where only a flat cell separates capture from an
  addition-rival (probe chosen, rival separated); a pair where a non-flat
  cell already separates them (no probe used, last pass never needed to
  fire); flat cells absent from the stable/wide pool by construction (a
  candidate list of only flat cells cannot be resolved by anything but the
  last pass); and a probe on a cell a third, genuine variant frame changes
  (excluded by the same stability filter as any other candidate, so no
  probe is ever chosen).
- `Core/Core.vcxproj`/`.filters` — untouched (no new files; `ScreenStitcher`
  and `HdPackBuilder` already registered).

## `make core-unit-tests`

982 before this slice; **990 after**, the 8 new BlocoP5 cases all `PASS`.
The 8 `BlocoE` failures seen in an earlier pass (`python3 scripts/mep_recipe.py
apply`, "relative path sets differ (6 vs 0 files)") were a subprocess `PATH`
resolution issue in this sandboxed environment (the test harness's spawned
`python3` resolving to the wrong interpreter), not a regression: re-run once
with `/Library/Frameworks/Python.framework/Versions/3.12/bin` placed first on
`PATH`, all **990/990** cases pass, `BlocoE` included.

## Binaries proven

- `InteropDLL/obj.osx-arm64/MesenCore.dylib`, built in this worktree
  (CommandLineTools clang, `-isysroot`), sha256
  `e70788c86fed3b430bc7523b140455449b9e60892e488260b89a1df3f4a94ebf`;
  `grep -a -c "gated on an emptiness probe"` -> 1 (the literal is compiled
  in; the log line itself was emitted at record time, see below).
- `scripts/headless_record`, linked against the dylib above by absolute
  path (`install_name_tool -change` + ad hoc `codesign`), sha256
  `f03ecacfe532cade0711f9bab9b7c0faa5923726c18e62b4ce5b3da4fab1104b`.
- No `main`-checkout ("before") dylib was built in this session (time
  budget) — the before/after comparison below leans on ADR-0223's own
  prototype numbers, not a second binary in this pass. Flagged in "Not
  verified".

## The recording

ADR-0221's route — 60 s from power-on, `bootstrap`, no input, ROM copied to
a scratch directory so no sibling pack is discovered; grid dump on:

```
MESEN_SHEET_GRID_DUMP=<d>/grid.txt <headless_record> <d>/game.nes 60 <d>/run bootstrap log
```

10 screens captured, 592 retained grid frames / 3 351 played. The summary
line (`<d>/mesen-home/mesen.log`, `[HDPack] bootstrap:`):

```
10 screen(s) anchored (4 on a cell a variant may change, 6 still matching
another recorded screen, 1 skipped for an earlier capture's gate, 0 dropped
post-hoc, 228 frame(s) filed as rivals for adding content the capture lacks,
6 screen(s) gated on an emptiness probe)
```

9 `<background>` lines survive (one, `screen010`, collides with an earlier
capture's gate exactly as in F12.13/F12.15 — unrelated to this slice).
`screen003`–`screen006` each carry a probe on tile `FD` at rows 120/136/176/200
— the card's flat backdrop, exactly the cells ADR-0223's prototype names.
The pack also carries `<bgPreservesBehindBgSprites>` (F12.15's tag, written
unconditionally by the recorder since that slice shipped).

## Stop condition (1) — `erased background` = 0 on the card

Full 36-point, 1-s-step sweep (10 s .. 45 s), each timestamp rendered twice
(`none` — `hdpack-off`; `after` — the pack above), pack installed at
`<romdir>/game/auto`, each timestamp+config in its own working directory (so
`measure_capture_overdraw.py --sweep` finds one `Screenshots/` per point):

```
<headless_record> <d>/game.nes <t> ./run screenshot log [hdpack-off]
python3 scripts/measure_capture_overdraw.py --sweep <sweep> --baseline-prefix none \
  --render-prefix after --grid <rec>/grid.txt --sprite-height 16 --verdict background \
  --json overdraw.json
```

| s | detail | erased | background | sprite | behind-bg |
|---|---|---|---|---|---|
| 10 | 701 | 0 | 0 | 0 | 0 |
| 11 | 398 | 0 | 0 | 0 | 0 |
| 12 | 398 | 0 | 0 | 0 | 0 |
| 13 | 398 | 0 | 0 | 0 | 0 |
| 14 | 398 | 0 | 0 | 0 | 0 |
| 15 | 398 | 0 | 0 | 0 | 0 |
| 16 | 398 | 0 | 0 | 0 | 0 |
| 17 | 398 | 0 | 0 | 0 | 0 |
| 18 | 77 | 0 | 0 | 0 | 0 |
| 19 | 87 | 0 | 0 | 0 | 0 |
| 20 | 94 | 0 | 0 | 0 | 0 |
| 21 | 96 | 0 | 0 | 0 | 0 |
| 22 | 110 | 0 | 0 | 0 | 0 |
| 23 | 116 | 0 | 0 | 0 | 0 |
| 24 | 129 | 0 | 0 | 0 | 0 |
| 25 | 139 | 0 | 0 | 0 | 0 |
| 26 | 148 | 0 | 0 | 0 | 0 |
| 27 | 148 | 0 | 0 | 0 | 0 |
| 28 | 333 | 0 | 0 | 0 | 0 |
| 29 | 147 | 0 | 0 | 0 | 0 |
| 30 | 159 | 0 | 0 | 0 | 0 |
| 31 | 172 | 0 | 0 | 0 | 0 |
| 32 | 184 | 0 | 0 | 0 | 0 |
| 33 | 195 | 0 | 0 | 0 | 0 |
| 34 | 195 | 0 | 0 | 0 | 0 |
| 35 | 0 | 0 | 0 | 0 | 0 |
| 36 | 701 | 0 | 0 | 0 | 0 |
| 37 | 701 | 0 | 0 | 0 | 0 |
| 38 | 701 | 0 | 0 | 0 | 0 |
| 39 | 701 | 0 | 0 | 0 | 0 |
| 40 | 0 | 0 | 0 | 0 | 0 |
| 41 | 378 | 0 | 0 | 0 | 0 |
| 42 | 378 | 0 | 0 | 0 | 0 |
| 43 | 386 | 0 | 0 | 0 | 0 |
| 44 | 402 | 0 | 0 | 0 | 0 |
| 45 | 402 | 0 | 0 | 0 | 0 |

`0 of 36 frames lose ROM content; 0 erased cells in total` (tool's own
summary): whole-sweep totals **`erased background` = 0, `erased sprite` = 0**
across all 36 points. F12.13's log (old dylib, no tag) reads **437** erased
cells total (374 background + 63 sprite) on the same route; F12.15's log
(new dylib, `<bgPreservesBehindBgSprites>` tag on) reads **374** (374
background + 0 sprite). F12.16's pack also carries the F12.15 tag, so this
run's 0/0 is the further fall on top of F12.15's 374 -> 0, and the same run
cannot separate how much of that last fall is F12.16's emptiness probes
alone versus the tag already suppressing the residue F12.15 traced — see
"Not verified". **Card window (19–27 s): `erased background` = 0 — stop
condition (1) met** on the full sweep, not just the reduced 14-point window
measured in the first pass.

## Stop condition (2) — unit tests

Met; see "What shipped" and "`make core-unit-tests`" above.

## Stop condition (3) — 30-ROM library re-record

Done. "Before" reuses F12.13's after-side packs, still present at
`scratchpad/f1213/lib-after/` (confirmed: 30 ROM directories, all 30 with a
`hires.txt` and a `done` marker) — these were not re-recorded. "After" is a
fresh 30-ROM record with this slice's dylib
(`f1216lib_record.sh`, `caffeinate -dimsu`, same route: 60 s, `bootstrap`,
no input, ROM copied to a scratch dir per ROM), aggregated with
`f1216lib_aggregate.py` (`measure_capture_draw_rate.py --rom` for mean draw
rate; `mesen.log`'s `[HDPack] bootstrap:` line for the new "screens gated on
an emptiness probe" counter; a `<condition>`-line diff of each ROM's
before/after `hires.txt` for the condition-change count):

| ROM | captures before | after | mean draw rate before | after | never-firing before | after | probes (after) |
|---|---|---|---|---|---|---|---|
| 1942 (1985) (Capcom) | 0 | 0 | 0.0000 | 0.0000 | 0 | 0 | — |
| Bomberman (1985) (Hudson Soft) | 16 | 16 | 0.0507 | 0.0510 | 0 | 0 | 3 |
| Bubble Bobble (1987) (Taito) | 1 | 1 | 0.9325 | 0.9325 | 0 | 0 | 0 |
| Castlevania (1987) (Konami) | 21 | 21 | 0.0382 | 0.0382 | 0 | 0 | 0 |
| Contra (1988) (Konami) | 3 | 3 | 0.0884 | 0.0884 | 0 | 0 | 0 |
| Donkey Kong (1983) (Nintendo) | 30 | 30 | 0.0439 | 0.0439 | 0 | 0 | 0 |
| Double Dragon (1988) (Technos) | 2 | 2 | 0.4962 | 0.4962 | 0 | 0 | 0 |
| Dr. Mario (1990) (Nintendo) | 1 | 1 | 0.5683 | 0.5683 | 0 | 0 | 0 |
| Excitebike (1984) (Nintendo) | 2 | 2 | 0.2251 | 0.2249 | 0 | 0 | 1 |
| F-1 Race (1984) (Nintendo) | 5 | 5 | 0.1546 | 0.1546 | 0 | 0 | 0 |
| Gauntlet (1988) (Tengen) | 2 | 2 | 0.0179 | 0.0179 | 0 | 0 | 0 |
| Golf (1984) (Nintendo) | 7 | 7 | 0.1429 | 0.1429 | 0 | 0 | 0 |
| Ice Climber (USA, Europe, Korea) | 4 | 23 | 0.3199 | 0.1952 | 0 | 0 | 23 |
| Lemmings (1993) (Sunsoft) | 1 | 1 | 0.6856 | 0.6856 | 0 | 0 | 0 |
| Lifeforce (1988) (Konami) | 1 | 1 | 0.1366 | 0.1366 | 0 | 0 | 0 |
| Mario Bros. (1983) (Nintendo) | 3 | 3 | 0.3324 | 0.3434 | 0 | 0 | 1 |
| Mega Man (1987) (Capcom) | 1 | 1 | 1.0000 | 1.0000 | 0 | 0 | 0 |
| Mega Man 2 (1988) (Capcom) | 13 | 13 | 0.0525 | 0.0525 | 0 | 0 | 0 |
| Metroid (USA) | 11 | 11 | 0.0843 | 0.0843 | 0 | 0 | 0 |
| Mike Tyson's Punch-Out!! (1987) (Nintendo) | 10 | 9 | 0.1200 | 0.0865 | 0 | 0 | 6 |
| Ninja Gaiden (1989) (Tecmo) | 4 | 4 | 0.0771 | 0.0771 | 0 | 0 | 0 |
| Pac-Man (1984) (Namco) | 17 | 25 | 0.2219 | 0.0916 | 0 | 0 | 21 |
| Super Mario Bros. (1985) (Nintendo) | 1 | 1 | 0.4423 | 0.4423 | 0 | 0 | 0 |
| Super Mario Bros. 3 (1988) (Nintendo) | 4 | 4 | 0.0467 | 0.0467 | 0 | 0 | 0 |
| Tennis (1984) (Nintendo) | 5 | 5 | 0.2283 | 0.2470 | 0 | 0 | 1 |
| Tetris (1989) (Nintendo) | 17 | 17 | 0.0679 | 0.0679 | 0 | 0 | 1 |
| Tetris 2 (1993) (Nintendo) | 4 | 4 | 0.2507 | 0.2507 | 0 | 0 | 0 |
| The Flintstones - The Surprise at Dinosaur Peak! (1994) (Taito) | 3 | 3 | 0.4778 | 0.3236 | 0 | 0 | 1 |
| The Legend of Zelda (1987) (Nintendo) | 4 | 4 | 0.0593 | 0.0593 | 0 | 0 | 0 |
| Zelda II - The Adventure of Link (1988) (Nintendo) | 0 | 0 | 0.0000 | 0.0000 | 0 | 0 | — |
| **total / mean** | **193** | **219** | **0.2454** | **0.2316** | **0** | **0** | **58** |

Captures rose overall (193 -> 219, driven by Ice Climber 4 -> 23 and Pac-Man
17 -> 25 both gaining a probe-separated split; Punch-Out!! is the one ROM
that lost a capture, 10 -> 9, the same pre-existing gate collision noted
above). Mean draw rate across scored ROMs **fell slightly, 0.2454 -> 0.2316**
— reported, not a stop condition per the coordinator's instruction. Never-
firing stayed 0 -> 0 throughout. **58** screens gated on an emptiness probe,
summed across the 9 ROMs where the last pass fired (Bomberman, Excitebike,
Ice Climber, Mario Bros., Punch-Out!!, Pac-Man, Tennis, Tetris, The
Flintstones); 21 of the 30 ROMs never needed the last pass. **13 of 30**
packs changed at least one `<condition>` line between before and after.

## Not verified

- No pre-F12.16 dylib was built fresh in this worktree to reproduce the
  "before" side of either measurement directly. The sweep's "before" figure
  (437, F12.13's old-dylib-no-tag log) and the library's "before" side
  (F12.13's already-recorded `lib-after/` packs) are both taken from a prior
  session's binaries/artifacts, not rebuilt and re-measured against source
  in this pass — only the "after" (F12.16) side was built and measured here.
  Both are traceable (sha256s above for the after-side binaries; the
  before-side packs still exist on disk and were confirmed present), but
  they were not independently reproduced.
- The full-sweep 0/0 total cannot be attributed solely to this slice: the
  pack recorded also carries F12.15's `<bgPreservesBehindBgSprites>` tag, so
  the run measures the two mechanisms stacked, not F12.16 in isolation
  against a tag-off baseline.
- The library table's "packs whose `<condition>` lines changed" (13 of 30)
  is a line-level diff of `hires.txt`, not a semantic diff — it does not
  distinguish a probe's `<tileAtPosition>` line being added from some other,
  unrelated builder change touching the same pack between the two sessions'
  binaries (F12.13's dylib and this slice's).

## Deviations from the ADR/PRD

None known. `AppendFlatAnchorCells` living in the header (rather than
`ScreenStitcher.cpp`, which is host-free) is a placement choice forced by
`ScreenRun` being an `HdPackBuilder`-private type and the ADR-0137 ceiling
leaving zero headroom in `HdPackBuilder.cpp`; the arithmetic it depends on
(`FlatRunColumns`) is in `ScreenStitcher.cpp` as intended.

## Independent verification (Sonnet, same day)

Reproduced from the artefacts, not re-quoted: 990/990, `HdPackBuilder.cpp`
at 2428, 9 `<background>` lines, the four `FD` probes at rows 120/136/176/200,
the recorder line ("6 screen(s) gated on an emptiness probe", "228 frame(s)
filed as rivals"), `erased background` = 0 on all 36 sweep points, 9 captures
at 0.0865, and the Ice Climber (4 -> 23) / Pac-Man (17 -> 25) counts — the new
captures pixel-diff as distinct backgrounds (platform / dot-state changes),
not one screen fragmented. Verdict PASS, all three stop conditions met.

On the isolation caveat above: the F12.15 tag only gates
`DrawBehindBgSprites`, so it can move the `erased sprite` column and never
the `erased background` one. The 374 -> 0 on `erased background` is therefore
F12.16's alone, column-wise, without a second binary; the caveat stands only
for the sweep *total*.
