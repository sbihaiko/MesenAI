# Upstream sync of 3924215 (issue #429), 2026-09-24

- Upstream: `nesdev-org/MesenCE` `master` @ `3924215009afbd5e524a24145acda2484a4ceaf8`
  (not moved past the SHA the issue named; fetched 2026-09-24)
- Merge-base: `0636c265d441cfd4302da5c2e3828c1b49aeb70a`
- Fork side: `main` @ `b1c62733`
- Commits brought: `4fd87323` "Debugger: Fixed crash when loading ELF files (#280)"
  and `39242150` "Added support for shaders (Windows + Linux) (#270)"
  (librashader, OpenGL renderer on Linux instead of SDL, WASAPI on Windows,
  DirectXTK removed, SDL2 loaded at runtime via `dlopen` instead of linked).
- Run: `scripts/sync-upstream.sh --ref upstream/master` (ADR-0163).

## Tier D (delete/modify)

The script auto-resolved 10 conflicts by keeping the fork's deletion:
`UI/Config/{CvConfig,PcEngineConfig,SnesConfig,WsConfig}.cs`,
`UI/ViewModels/{OtherConsoles,Pce,Snes,Ws}ConfigViewModel.cs`,
`UI/Views/SnesConfigView.axaml(.cs)`. Nothing was restored. Upstream's edit to
those files (moving `Video.ApplyConfig()` from each console's `ApplyConfig()`
into its view-model observer) reached the surviving consoles (GB, GBA, NES,
SMS) through clean merges, so no deleted-console behavior was lost.

## Tier B (modify/modify)

| File | Fork side | Upstream side | Resolution |
|---|---|---|---|
| `.github/workflows/build.yml` | ADR-0203 matrix, ADR-0204 publish job, comment on the Windows MSBuild step, `make doc-checks` step on Linux | "Get librashader library" step (new composite action) before each build, `libraUrl` per Linux platform | Both. The librashader step goes before the fork's MSBuild comment on Windows and after `make doc-checks` on Linux; the matrix lines and the AppImage job merged clean. macOS gets no librashader step (upstream ships shaders on Windows + Linux only). |
| `Core/Shared/EmuSettings.cpp` | `SetAudioConfig` also copies `EnhancedAudioSoundFontPath` | `SetAudioConfig` re-creates the audio device when `AudioBackend` changes | Both lines kept. |
| `Core/Shared/Emulator.cpp` | includes `MepPackManager.h`, `LiveFrameRecorder.h` | dropped the `MemoryOperationType.h` include | Fork's include list kept (the fork's includes are needed; the dropped one is harmless). |
| `InteropDLL/EmuApiWrapper.cpp` | `InteropHdPackCoverageReport` struct (F5.4d) | `InitRenderer()`/`InitSoundManager()` factories, the devices now owned by `Emulator` | Both. **Adapted:** upstream passes the callbacks crossed, `SetAudioVideoInitCallback(noVideo ? nullptr : InitSoundManager, noAudio ? nullptr : InitRenderer)`, so `noVideo` gated audio and `noAudio` gated video. The merge passes `noAudio` for audio and `noVideo` for video. The fork's tools pass both flags true, so they behave the same either way, but the UI path depends on each flag gating its own device. |
| `UI/Config/Configuration.cs` | removed the CV/WS input-upgrade blocks (consoles dropped) | added the `WindowsAudioLatency` upgrade (30 ms on WASAPI) | Upstream block kept, CV/WS blocks stay removed. The `ConfigUpgradeHint` enum keeps `CvInput`/`WsInput`, so the ordinals match upstream. |
| `UI/Debugger/Integration/ElfImporter.cs` | `9d3c1414` guard: `int.TryParse` + `break` | same fix (#280) plus `val > 0`, `_Z`-anchored start, nested-name `E` stop | Upstream's version taken; it covers the fork's guard. |
| `UI/Utilities/FileDialogHelper.cs` | `MidiExt`, `VgmExt` | `ShaderExt` | All three kept. |
| `UI/Windows/MainWindow.axaml.cs` | P.7: the fit comes from `RendererViewportFit` (UI/Logic, asserted by `RendererViewportFitTests` and `RendererLetterboxTests`) | even `realWidth/realHeight` (round up, shader artifact), width/height snapped to whole device pixels, manual centering in a `Canvas`, `ConfigApi.CheckShaderSupport()` | Fork's fit kept; upstream's centering and `CheckShaderSupport()` taken. **Held back, needs a decision:** the even-size round-up and the device-pixel snap. See below. |
| `makefile` | `-MMD -MP`, unit-test/tool targets, `check-manifest`, `clean` of the test objects | header comment, SDL2 no longer linked (`SDL2INC`/`SDL2LIB`/`X11LIB` dropped; `Sdl/include/Sdl2.h` `dlopen`s it), `clean` removes `$(OUTFOLDER)` | Upstream's header comment and dropped SDL flags. `-MMD -MP` kept in `CXXFLAGS`. Both `clean` lines. |

## Open decision: renderer size rounding (MainWindow)

Upstream rounds the physical renderer size up to even numbers and then sets
the control's logical size from those rounded pixels. Both changes break the
fork's headless contract, and at least one assertion fails whichever way the
rule is applied:

- round up to even (upstream): the renderer comes out 376 px tall in a 375 px
  panel, so it overflows the panel (`The_picture_is_letterboxed_inside_the_panel_and_keeps_its_ratio`)
  and no longer matches the shared fit (`The_renderer_is_sized_by_the_shared_fit_rule`,
  `Force_integer_scale_does_not_apply_to_a_normal_window`);
- round down to even inside `RendererViewportFit` (tried): 402x374 misses the
  core's aspect ratio at the 3-decimal precision the letterbox test asserts
  (1.075 vs 1.071);
- device-pixel snap alone: 402.00001 vs the fit's 401.674, whenever the aspect
  gives a fractional width. Which aspect is in effect depends on whether a
  parallel test has a ROM loaded, the same parallelism as #432, so this one
  fails only some of the time.

The merge keeps the fork's sizing (every headless test green) and marks the
held-back lines with a comment in `RendererPanel_LayoutUpdated`. Nothing is
lost on the fork's main platform, because upstream's shaders are Windows + Linux
only and macOS keeps the software renderer. To take the upstream behavior
later: move the even-size rule and the snap into `RendererViewportFit` (P.7
geometry lives there), cover them in `RendererViewportFitTests`, and loosen the
headless assertions to one device pixel.

## Other notes

- SDL2 is now loaded at runtime. On macOS `Sdl2.h` looks in
  `/opt/homebrew/lib`, `/usr/local/lib` and `/opt/local/lib`. A Mac without
  Homebrew SDL2 now runs without audio, where before the core failed to load.
- No Core `.cpp` was added, so the ADR-0007 manifest check is unaffected. The
  new C++ sources are all Windows/Linux-only (`Windows/`, `Linux/`) or
  header-only (`Utilities/Video/librashader*.h`).

## Verification (worktree, macOS arm64)

- No conflict markers; no unmerged paths.
- `make core`: pass (after touching the four resolved C++ files).
- `make core-unit-tests`: 1053/1053.
- `dotnet test UI.Tests`: 516/516.
- `make headless-ui-tests`: 20 passed, 4 skipped (env-gated), 0 failed, three consecutive runs.
- `make python-tests`: 57/57.
- `make doc-checks`: pass.
- Not run: Windows/Linux builds (`build.yml` is dispatch/`prod`-PR only). The
  librashader download step is new third-party network surface in CI
  (`nightly.link/SourMesen/librashader`).
