# F12.2 — *Copy as MEP sheet cell* dispatcher script (not the evaluator briefing)

This is the script the **dispatcher** executes. The evaluator is a fresh Fable
session (ADR-0214, go-ahead 2026-09-19: *"quero usar o Fable, ajuste o que for
necessário"*). Handing **this** file to Fable invalidates the run: it is the
answer key. The evaluator-facing document is
`docs/validation/f12.2-fable-evaluator-briefing.md`.

The F12.2 row still names a cold-read paste-and-paint without reading
`hires.txt`. ADR-0214 assigns that read to Fable; criteria 8 and P15 stay
observations; pointer-level discoverability (hover vs click, flyout AX) stays
not evaluated until a pointer harness exists.

Fill a dated copy — `f12.2-fable-panel-<game>-<YYYY-MM-DD>.md` — from the
evaluator's verbatim log, in the shape the C.5 Zelda log uses: a **Binary and
inputs** header (including the label dump from S5), the log with clock times,
the results table, then **Defects filed**.

## Rules for the dispatcher (do this; do not tell Fable the path)

- Do not paste this file, the PRD, any ADR, `scripts/*.py` source, or the
  mechanical replay into the Fable session. The sandbox is the C.5 sandbox
  plus this briefing and the work copies.
- Before the session starts, run S1–S5. Put `tilemap-menu-labels.txt` in
  the Fable sandbox (the briefing names that file) and also paste it into
  the log's Binary and inputs header.
- After the session, score the table from the verbatim log. A `hires.txt`
  read is a fail of criterion 4 and cannot be scored as acceptance even if
  magenta appeared (ADR-0214 §4). P15 is an observation.
- Repeat the whole run for both games.

## Setup — done before the evaluator sits down, not on their clock

Five things will silently defeat the run if skipped. S1–S3 were found while
drafting this script; S4 was found on the first attempt at the run itself;
S5 is the label dump ADR-0214 §3 needs, because the evaluator cannot open
the flyout.

**S1 — the packs must be re-recorded first. Done 2026-09-17; the replacements
are described at the end of this file.** The `auto/` packs currently
installed beside both ROMs do **not** rebuild: `mep_build.py build` exits **2**
with 13 errors on Zelda (`spr004`, `spr005`, `spr007`, …) and 24 on Contra,
each reading *"crop(s) carry a flip-baked tile key the run time never looks up
… re-record the pack with a build that has ADR-0178"*. Both recordings predate
ADR-0178. Re-record both games with the current binary and confirm
`python3 scripts/mep_build.py build <pack>` exits 0 on each **before** the
panel starts, or the evaluator hits a hard refusal at P13 with no way forward.
This is not a defect: ADR-0178 changed the contract and the refusal is the
correct behaviour — re-recording is what the error message asks for, and the
user's decision (2026-09-17) is to re-record rather than relax the gate.

**S2 — give the evaluator work copies, not the live packs.**

- `~/f12.2-panel/zelda/` ← `<roms>/The Legend of Zelda (1987) (Nintendo)/auto`
- `~/f12.2-panel/contra/` ← `<roms>/Contra (1988) (Konami)/auto`

**S3 — move the community pack aside.** Both game folders carry an
auto-installed `mep/` (Zelda `pack_id issue-139`), and `mep/` wins over `auto/`
entry by entry (ADR-0147). Painting in `auto/` and reopening the ROM would
render the community art instead, with nothing on any surface saying so — the
exact failure shape C.5 hit. Rename `<rom dir>/mep` to `<rom dir>/mep.off` in
both folders and set `AutoInstallCommunityPacks` to `false` in Preferences.


**S4 — clear stray packs out of `EnhancementPacks/`. Done 2026-09-17.** A
leftover synthetic pack can win the `textures` section over the sibling
`auto/` folder and leave the ROM dressed in nothing at all. On the first
attempt at this run the log read:

```
[MEP] pack 'The Legend of Zelda (1987) (Nintendo)' v0.0.0 [folder convention] matches ROM sha1 B6643CE5...
[MEP] textures section has no loadable hires.txt in .../EnhancementPacks/Zelda 150 Test A/textures
[MEP] LoadHdPack: 0 ms; tiles=0 keys=0 images=0 backgrounds=0
[MEP] bootstrap: nothing was recorded - 'Zelda 150 Test A' already dresses this ROM, so the bootstrap kept it as it was.
```

`Zelda 150 Test A` and `Zelda 150 Test B` were two-file stubs (`pack.json`
plus a single `textures/0.png`, no `hires.txt`) left behind by an earlier
scale test. They still matched the ROM well enough to take the section, and
the bootstrap then declined to record because it believed the ROM was already
dressed — so nothing rendered and P14 could not have passed. Both were moved
to `~/f12.2-panel/stray-enhancementpacks/`. After the move the same ROM loads
`tiles=2807 keys=3782 images=19 backgrounds=15` with a 100% bg tile match
rate.

Before the panel, list `~/Library/Application Support/MesenCE/EnhancementPacks/`
and move aside anything that is not a real submission. Then confirm the pack
actually loads — `./scripts/headless_record <rom> 3 <prefix> log` and check for
a non-zero `tiles=` on the `[MEP] LoadHdPack` line. A green-looking emulator
window is not evidence; that line is.

**S5 — dump the visible, enabled Tilemap Viewer context-menu labels
(ADR-0214 §3).** The Fable session cannot open the flyout. Produce the list
the briefing treats as a right-click:

```
MESEN_NES_ROMS=<library> MESEN_F122_LABEL_DUMP=<log-dir>/tilemap-menu-labels.txt \
  make headless-ui-tests
```

The dump is written by `CopyAsMepSheetCellTests` from `action.Name` of every
visible, enabled, non-separator entry after a tile is selected. Copy the
file into the Fable sandbox as `tilemap-menu-labels.txt` and paste the same
contents into the log header. If the dump does not contain both
`Copy as MEP sheet cell` and `Copy tile (HD pack format)`, stop — the
evaluator has nothing honest to choose from.

## Run (answer key — dispatcher only)

The evaluator is **not** given P1–P14. They get the briefing. The numbered
steps below are what a successful supported path looks like, so the
dispatcher can score, and so a person repeating the run later has the same
map. They are the 2026-09-18 path including the P5/P6 corrections.

P1. Launch the emulator (`make ui`, or the released app). You see the Player
shell: a game list / welcome card, no menu bar.

P2. Press **Esc** for the thin overlay and click **Advanced GUI**. A classic
menu bar appears: `File  Game  Settings  Tools  Debug  Help`. (The third
item was written `Options` until 2026-09-17; the resource `mnuOptions`
renders as "Settings".)

P3. `File > Open` and load `The Legend of Zelda (1987) (Nintendo).nes` or
`Contra (1988) (Konami).nes`. The game boots and runs.

P4. Play to a screen showing a shape you want to repaint — a bush or rock on
Zelda's first overworld screen, a ground or wall tile on Contra's stage 1.
Press **Esc**. The picture freezes.

P5. `Debug > Tilemap Viewer` (**Ctrl+1**). A window opens showing the full
nametable as the paused frame draws it. There is **no grid** on it: `ShowGrid`
in `UI/Config/Debugger/TilemapViewerConfig.cs` has no initialiser, so
`Show tile grid` starts unchecked. Tick it if you want one — nothing below
depends on it. (Corrected 2026-09-18; the step claimed the grid was already
drawn.)

P6. **Left-click** the shape you picked. A panel appears on the right, below
the `Tilemap` block, with that tile's address, palette index and a magnified
preview. Confirm the preview is the shape you meant.

The click is required, not a convenience: `UpdatePreviewPanel()` in
`UI/Debugger/ViewModels/TilemapViewerViewModel.cs` sets `PreviewPanel = null`
whenever `SelectionRect == default`, and only a click fills `SelectionRect`.
Hovering feeds `ViewerTooltip` instead — a floating tooltip that follows the
cursor. Until you click, the right-hand side shows only the whole-screen
`Tilemap` block (Size, Tilemap Address, Tileset Address, …), which is easy to
mistake for "the panel is broken". (Corrected 2026-09-18; the step said
"hover", and the first evaluator hit exactly that.)

P7. **Right-click that tile.** The menu ends with a separator and two entries:

```
Copy tile (HD pack format)
Copy as MEP sheet cell
```

Click **Copy as MEP sheet cell**. *Nothing visible happens* — no toast, no
status line, no highlight. The clipboard is the only evidence.

P8. Paste into a scratch text file to see what you got. It must be one line of
this shape (real keys from the shipped unit tests):

```
{"tile": "384CC6C6C66438000000000000000000", "palette": "0F300012"}
```

Both bounded games are CHR RAM, so there is no `"index"` field; on a CHR ROM
game the same action emits a third field, `"index": 2`.

P9. Open `~/f12.2-panel/<game>/textures/sheets/misc.json` in a plain text
editor. You see `"columns"`, `"cell": {"w":16,"h":16}`, `"gutter": 1` and a
`"cells"` array. Scroll to the end of `"cells"`.

P10. Append one cell, pasting the clipboard text as the **first** entry of
`tiles` — the three `null`s are the cell's other three 8×8 quadrants, which
have no art. Mind the comma before the new object, and save. **The two games
differ here**; take your game's line.

Zelda — the sheet has 23 cells in a 5×5 grid, so slot 23 is free:

```json
{"index": 23, "x": 52, "y": 69, "count": 1, "context": "misc", "tiles": [
  {"tile": "384CC6C6C66438000000000000000000", "palette": "0F300012"}, null, null, null]}
```

Contra — the sheet has exactly 20 cells in 4 full rows of 5 and **no free
slot**, so the new cell opens a fifth row at `x 1, y 69`:

```json
{"index": 20, "x": 1, "y": 69, "count": 1, "context": "misc", "tiles": [
  {"tile": "003FFFFFFF7F84D10000000000807B2E", "palette": "0F192908"}, null, null, null]}
```

P11. Open `~/f12.2-panel/<game>/textures/sheets/misc.png` in your image editor.
The picture is a grid of 64×64 upscaled cells with a 4 px gutter.

- **Zelda** — `misc.png` is 344×344 and the bottom row has empty space. Nothing
  to resize.
- **Contra** — `misc.png` is 344×276 and every cell is occupied. Extend the
  canvas by one whole row, to 344×**344**, *and* extend the twin
  `misc.orig.png` from 86×69 to 86×**86**. Both, and by a whole row: an
  in-between height fails the build with `error: misc.png: 344x340 is not an
  integer multiple of the 86x86 sheet misc.json describes`, and growing only
  `misc.png` builds but logs `info: reference twin misc.orig.png is 86x69, not
  86x86 — every cell counts as painted`, which makes the whole sheet override.

P12. Fill a **32×32 px** square of solid magenta at the top-left corner of the
cell you just declared, and save the PNG.

- **Zelda** — paint at pixel `208, 276`. Do **not** touch `misc.orig.png`: that
  twin is what tells the builder your cell was painted.
- **Contra** — paint at pixel `4, 276`. `misc.orig.png` was already resized in
  P11, which is the one exception to the rule above; do not paint into it.

P13. Build:

```
python3 scripts/mep_build.py build ~/f12.2-panel/<game>
```

You see `info:` lines, no `error:`, and the command exits 0.

P14. Copy `~/f12.2-panel/<game>/` to `<rom dir>/mep/`, then `File > Open` the
same ROM again (F12.3 live-reloads a repainted image in place; this panel
still reopens so the clock stops on a known-good load, not on a cache hit).
Play back to the same screen. **The shape you right-clicked in P7 is now a
magenta square.** Stop the clock.

P15. Write down: elapsed minutes, how many times you looked something up and
where, whether you ever opened `hires.txt`, and in one sentence whether picking
a key this way beats typing it from a spreadsheet.

## Results

| # | Criterion | Pass condition | Zelda 1 | Contra |
|---|---|---|---|---|
| 1 | The action is findable | The evaluator names *Copy as MEP sheet cell* from the S5 label dump without being told which line, under 2 min from pause. Pointer/AX discoverability is not evaluated (ADR-0214 §3) | | |
| 2 | The clipboard text is usable as-is | The pasted text is valid JSON, one line, and needs no hand-editing before it goes into `tiles[]` (adding the surrounding comma does not count) | | |
| 3 | Paste-and-paint reaches the screen | After P13/P14 the right-clicked tile renders magenta in the running game | | |
| 4 | `hires.txt` never opened | The evaluator opened no `hires.txt` at any point. Any read is a fail, logged with its clock time and the question it answered | | |
| 5 | Round-trip is machine-checkable | `mep_build.py build` exits 0 **and** the copied key appears in the rebuilt `textures/hires.txt` as a `<tile>` whose `x,y` is the painted cell — see "The stop rule names a command that does not exist" below | | |
| 6 | Lint stays clean | `python3 scripts/mep_lint.py ~/f12.2-panel/<game>` exits 0 | | |
| 7 | Elapsed | P1→P14 under 20 min per game, recorded per step | | |
| 8 | Observation, not a gate | Did the evaluator expect feedback after clicking? Did they know whether the copy had worked before pasting? Record verbatim | | |

Criterion 3 is the one the PRD row names. Criteria 1, 2, 5 and 6 support it;
4 is a gate (a `hires.txt` read cannot be scored as acceptance). 8 and P15
are observations (ADR-0214 §4). Pointer-level discoverability is not a pass.

## Why criterion 5 is written against `build`

The F12.2 row's stop rule reads *"Stop when the pasted text round-trips through
`mep_build.py --verify` on both."* There is no such flag. `scripts/mep_build.py`
accepts only `build`, `pack`, `rename-audio-id` and `check-coverage`; `verify`
is a subcommand of `scripts/mep_import.py`, where it checks a rebuilt imported
project against the legacy pack it came from — a different subject. The kit
generators' `--verify` is a third thing again (key conservation across kit
extraction).

What exists and does measure the round-trip is `mep_build.py build` itself: the
pasted key comes back out as a `<tile>` line pointing at the painted cell.
Verified on both bounded games while drafting this script:

- Zelda — `<tile>4,384CC6C6C66438000000000000000000,0F300012,208,276,1,N,0,0`,
  with `<img>sheets/misc.png` at index 4; `208,276` is exactly the painted crop.
- Contra — `<tile>2,003FFFFFFF7F84D10000000000807B2E,0F192908,4,276,1,N,0,2`,
  with `<img>sheets/misc.png` at index 2. (Corrected 2026-09-19: this
  read `140,276`, which agrees with neither P10's `x 1` nor P12's paint
  coordinate. The mechanical replay emits `4,276`.)

`check-coverage` does not fit: it proves nothing was *lost* against an earlier
baseline, and says nothing about a key that is newly *added*.

Criterion 5 is written against `build` on that basis, and the PRD row's stop
rule was reworded to match (user's decision, 2026-09-17): the round-trip is
`mep_build.py build` emitting the pasted key as a `<tile>` whose `x,y` is the
painted cell's crop, with `mep_lint.py` exiting 0. A machine-readable
`verify-cell` subcommand remains a possible follow-up slice, deliberately not
smuggled into this panel.

## Known traps in the shipped code

Found while drafting; each shapes a step above.

1. **No live reload.** "paints it" cannot be seen without closing and
   reopening the ROM. F12.3 is the slice that fixes this. Hence P14.
2. **The action gives no feedback at all.** On failure — a non-hex or
   wrong-length key, or a tile whose 16 bytes would run past the end of CHR —
   the code deliberately leaves the clipboard untouched, which is the same
   observable behaviour as success. The evaluator cannot tell *copied* from
   *refused* from *stale clipboard* without pasting. P8 exists only for this;
   criterion 8 records it.
3. **A 16 px-tall sprite copies two objects, not one.** With
   `isLargeSprite`, the bottom tile's object is appended on a second line with
   **no comma between them** — deliberate, and the code says why: the entry the
   paste lands next to decides whether a separator is needed, and an unasked-for
   comma is the one thing that breaks the sidecar's JSON. The PRD row says
   "pastes one cell", so this script routes the panel through the **Tilemap**
   Viewer. A Sprite Viewer pass on an 8×16 game is worth logging as a separate
   observation, never as criterion 3.
4. **Menu visibility is narrower than the row suggests.** The item is
   `IsVisible => CpuType == Nes` in all three viewers, and enabled only for
   `NesPpuMemory` / `NesChrRam` / `NesChrRom`. In the **Tile Viewer** the
   "Source" dropdown must be a CHR source or the item is greyed out. In the
   **Tilemap Viewer** `IsEnabled` checks only the memory type, not whether a
   tile is under the cursor — so the item can be clickable with nothing
   selected, and the click is a silent no-op leaving the previous clipboard in
   place. An evaluator can paste an earlier tile's key and never know. P8
   catches it.

## The re-recorded packs (2026-09-17)

Setup step S1 was executed. Both games were re-recorded with
`scripts/headless_record` sha256
`511ba70f459e87d407802303b9437fb19d746521595a0c01b7c6391627afa927`, linking
`MesenCore.dylib` sha256
`17fa23ef622f15893ac86ece637310329517a599b156eaeec88a070e3ded18dc`
(`make capture-tool` output), 120 emulated seconds each. Zelda used the
library's own `The Legend of Zelda (1987) (Nintendo).play.txt`; Contra has no
`.play.txt`, so its route is `scripts/stages/contra/mint-stage1.txt` and
`stage1-long.txt` concatenated, run from power-on. Both probe as `gameplay`,
not menu-only, and a 300 s re-run produced identical key counts — the routes
exhaust well before 120 s, so 120 s is the budget.

| | `build` | `error:` | `lint` | rules / unique keys | distinct `tileData` |
|---|---|---|---|---|---|
| Zelda | rc 0 | 0 | rc 0 | 2 203 / 2 203 | 1 615 |
| Contra | rc 0 | 0 | rc 0 | 2 276 / 2 276 | 2 064 |

Against the installed packs they replace, the new recordings carry 92.3 % of
Zelda's exact keys (100 % of its distinct `tileData`) and 97.4 % of Contra's
(98.0 % of `tileData`).

The ADR-0178 refusal is gone on merit, not dodged: counting sprite-sheet tile
entries whose key has no `<tile>` line in the pack's own `hires.txt` gives
**0/363** for the new Zelda and **0/562** for the new Contra, against 44/114
and 134/211 for the installed ones. The new sidecars carry no `source`/`mirror`
fields at all, because no OAM-flipped shape was interned by these routes — so
the detector passes because there is nothing baked, which is the right reason.

Both packs round-trip the panel's own paste-and-paint, simulated end to end
before this script was amended:

- Zelda — `<tile>4,384CC6C6C66438000000000000000000,0F300012,208,276,1,N,0,0`,
  `<img>sheets/misc.png` at index 4.
- Contra — `<tile>2,003FFFFFFF7F84D10000000000807B2E,0F192908,4,276,1,N,0,2`,
  `<img>sheets/misc.png` at index 2, after the one-row canvas growth P11
  describes.

Contra's full `misc` sheet is stable, not an artefact of one route: it was
re-recorded twice more, with a stage1-boss route and a stage1-water+long route,
and produced exactly 20 cells every time.
